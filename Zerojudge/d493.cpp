#include <iostream>
#include <cmath>
using namespace std;
int main()
{
    long long int a,b,sum;
    while(cin >> a >> b)
    {
        sum=pow(a,b);
        cout<<sum<<endl;
    }
    return 0;
}
